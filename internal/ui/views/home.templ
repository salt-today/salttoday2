package views

import "github.com/salt-today/salttoday2/internal/ui/components"
import "github.com/salt-today/salttoday2/internal/store"

templ Home(comments []*store.Comment) {
	@Page(true) {
		<h1>This is the homepage aka where you see comments</h1>
		<div>Probably some sort of form here to filter comments</div>
		<div id="comments" hx-post="/comments">
			This div is supposed to contain comments
			@components.CommentsListComponent(comments)
		</div>
	}
}
